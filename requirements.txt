pygame-ce>=2.5.0
# NOTE: use the pygame-ce distribution name, not "pygame".
# "pygame" 2.6.1 on PyPI has no prebuilt wheel for Python 3.14, so pip
# tries to compile it from source and fails (MSVC/distutils error).
# pygame-ce provides the same "import pygame" namespace and ships 3.14 wheels.
# 3D mode (best effort, install only if needed):
# ursina>=6.0.0
